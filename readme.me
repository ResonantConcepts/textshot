# install

1. install firebase cli:  `npm install -g firebase-tools`

2. init firebase: `firebase init`

# run

run locally: `firebase serve` (or just open the .html file)

deploy: `firebase deploy`



